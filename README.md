# Insider-Threat-Detection-System
This project is a desktop security application built with Electron that performs system-wide behavioral monitoring using keystroke dynamics. Instead of relying on passwords or biometrics like fingerprints, the system continuously verifies the active user based on how they type. 
